name: cleanup-madrid-ingest-staging

on:
  push:
    paths:
      - ".github/workflows/ingest-madrid-once.yml"

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Remove one-time Madrid staging files
        shell: bash
        run: |
          git config user.name "dataSec ingest bot"
          git config user.email "actions@users.noreply.github.com"
          git rm -r tmp_madrid_ingest .github/workflows/ingest-madrid-once.yml
          git commit -m "chore: clean Madrid ingest staging"
          git push
